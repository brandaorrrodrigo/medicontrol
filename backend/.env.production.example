# Production Environment Variables
# Copy this file and configure with your production values

# Server
NODE_ENV=production
PORT=3001
FRONTEND_URL=https://your-frontend.vercel.app

# Database (Supabase, Railway, or Neon)
DATABASE_URL=postgresql://user:password@host:5432/database

# JWT Secrets (GENERATE STRONG PASSWORDS!)
# Use: openssl rand -base64 32
JWT_SECRET=CHANGE-THIS-TO-A-RANDOM-32-CHAR-STRING
JWT_REFRESH_SECRET=CHANGE-THIS-TO-ANOTHER-RANDOM-32-CHAR-STRING
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Email (Gmail App Password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-digit-app-password
EMAIL_FROM=noreply@mediccontrol.com

# File Upload
UPLOAD_DIR=uploads
MAX_FILE_SIZE=5242880

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Local AI (Ollama)
# IMPORTANTE: MedicControl usa APENAS IA local, sem APIs externas
# Em produção, o Ollama deve estar rodando no mesmo servidor ou em servidor dedicado
# Exemplo Railway/VPS: instalar Ollama no container/servidor e usar localhost
# Se usar servidor separado: OLLAMA_BASE_URL=http://seu-servidor-ollama:11434
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1
OLLAMA_TIMEOUT=120000
